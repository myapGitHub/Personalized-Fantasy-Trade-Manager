

<div>{{error}}</div>
<h2>UserName: {{userId}}</h2>
<h1>User is Profile is Public</h1>

<div>
    <p>Friend Status:</p>
    {{#if requested}}
        <p>Request Sent</p>
    {{/if}}
    {{#if inbox}}
        <form id="accept-friend-request" method="POST" action="/search-user/{{userId}}/accept-request">
            <input type="hidden" name="accept" id="accept" hidden>
            <button type="submit">Accept</button>
        </form>
        <form id="decline-friend-request" method="POST" action="/search-user{{userId}}/decline-request">
            <input type="hidden" name="decline" id="decline" hidden>
            <button type="submit">Decline</button>
        </form>
    {{/if}}
    {{#if friends}}
        <form id="remove-friend-request" method="POST" action="/search-user/{{userId}}/remove-friend">
            <input type="hidden" name="remove" id="remove" hidden>
            <button type="submit">Remove</button>
        </form>
    {{/if}}
    {{#if send}}
        <form id="send-friend-request" method="POST" action="/search-user/{{userId}}/add-friend">
            <input type="hidden" name="sendRequest" id="sendRequest">
            <button type="submit">Add</button>
        </form>
    {{/if}}
</div>

<div id="all-workouts">
  <h1 id="all-workouts-title">{{reqUserId}} Workouts</h1>
  {{#if workouts}}
  {{#each workouts}}
  <div id="each-workout">
    <h2>{{foundWorkout.workoutName}}</h2>
    <h3>Type: {{foundWorkout.workoutType}}</h3>
    {{#each foundWorkout.exercises}}
    <p>Exercise Name: {{this.name}}</p>
    <p>Sets: {{this.sets}}</p>
    <p>Reps: {{this.reps}}</p>
    <p>Weight: {{this.weight}}</p>
    <p>Difficulty Rating: {{this.rating}}</p>
    <br>
    {{/each}}
    {{!-- <h3>Difficulty: {{foundWorkout.rating}}</h2> --}}
      <a href="/comments/{{foundWorkout._id}}">View Comments</a>
  </div>
  {{/each}}
  {{else}}
  <h2>User Doesnt have any workouts</h2>
  {{/if}}
</div>


<script src="/public/js/workout.js"></script>
<script src="public/js/Profile/friend.js"></script>